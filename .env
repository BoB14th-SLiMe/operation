# Elasticsearch (원격 대시보드 PC 주소)
# docker-compose.yml 내부의 ES가 아닌,
# 별도 대시보드 PC의 ES를 사용한다면 이 주소를 수정하세요.
# (예: ELASTICSEARCH_HOST=192.168.0.100)
# 지금은 로컬 ES를 사용한다고 가정합니다.
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200

# Parser Settings
CAPTURE_INTERFACE=veth1
